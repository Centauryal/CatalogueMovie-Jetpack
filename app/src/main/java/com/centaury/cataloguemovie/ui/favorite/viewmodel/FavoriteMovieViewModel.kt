package com.centaury.cataloguemovie.ui.favorite.viewmodel

/**
 * Created by Centaury on 11/23/2019.
 */
/*
class FavoriteMovieViewModel @Inject constructor(private val catalogueRepository: CatalogueRepository) :
    ViewModel() {
    val favoriteMovie: LiveData<Any>
        get() = catalogueRepository.favoriteMovies

    @Throws(ExecutionException::class, InterruptedException::class)
    fun getDetailFavMovie(id: Int): MovieEntity {
        return catalogueRepository.getDetailFavMovie(id)
    }

    fun deleteFavoriteMovie(movieEntity: MovieEntity?) {
        catalogueRepository.deleteFavMovie(movieEntity)
    }

}*/
